import { PlaqueDirective } from './plaque.directive';

describe('PlaqueDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaqueDirective();
    expect(directive).toBeTruthy();
  });
});
