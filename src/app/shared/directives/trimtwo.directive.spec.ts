import { TrimtwoDirective } from './trimtwo.directive';

describe('TrimtwoDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimtwoDirective();
    expect(directive).toBeTruthy();
  });
});
