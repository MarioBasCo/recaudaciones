
const protocol = 'http://'
const domain = '18.219.5.230';
const port = '';
const sufix = '/api';

export const environment = {
  production: true,
  baseUrl: `${protocol}${domain}${port}${sufix}`
};
